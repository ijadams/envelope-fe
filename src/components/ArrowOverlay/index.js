import React from 'react';

export const ArrowOverlay = (props) => {

    return (
        <div className={`nav--overlay ${props.active ? "active" : ""}`}>
            <h1>test</h1>
        </div>
    );
};
